<?xml version="1.0" encoding="UTF-8" ?>
<!-- See: https://github.com/phonegap/phonegap-start/blob/master/www/config.xml -->
<widget xmlns   = "http://www.w3.org/ns/widgets"
        xmlns:gap   = "http://phonegap.com/ns/1.0"
        id          = "com.gamingplatform.reversi"
        versionCode = "8"
        version     = "1.0.0" >
    <!-- versionCode is optional and Android only:
      needs to be increased every time you submit a new APK version.
      see: http://developer.android.com/tools/publishing/versioning.html
    -->

    <name>Reversi</name>

    <description>
        A multiplayer Reversi game.
    </description>

    <author href="http://yw1981.github.io/homepage/Reversi" email="yw1981.nyu@gmail.com">
        Yuxin Wang
    </author>

    <gap:config-file platform="ios" parent="CFBundleShortVersionString">
      <string>1000</string>
    </gap:config-file>

    <!--
        If you do not want any permissions to be added to your app, add the
        following tag to your config.xml; you will still have the INTERNET
        permission on your app, which PhoneGap requires.
    -->
    <preference name="permissions"                value="none"/>
    <feature name="http://api.phonegap.com/1.0/network" />

    <!-- Customize your app and platform with the preference element. -->
    <preference name="phonegap-version"           value="cli-5.2.0" />
    <preference name="orientation"                value="default" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="fullscreen"                 value="true" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="target-device"              value="universal" />      <!-- ios: possible values handset, tablet, or universal -->
    <preference name="prerendered-icon"           value="false" />          <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="detect-data-types"          value="false" />          <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend"            value="false" />          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="deployment-target"          value="7.0" />            <!-- ios: This sets the IPHONEOS_DEPLOYMENT_TARGET in the build, which tranlsates to the MinimumOSVersion in the ipa Propertly List. -->
    <preference name="webviewbounce"              value="true" />           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="stay-in-webview"            value="false" />          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="show-splash-screen-spinner" value="true" />           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="true" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="android-minSdkVersion"      value="7" />              <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

    <gap:config-file platform="ios" parent="UIViewControllerBasedStatusBarAppearance" overwrite="true">
        <false/>
    </gap:config-file>
    <gap:config-file platform="ios" parent="UIStatusBarHidden" overwrite="true">
        <true/>
    </gap:config-file>

    <gap:plugin name="org.apache.cordova.splashscreen" />
    <gap:splash src="splash.png" width="512" height="512"/>

    <icon src="icon512x512.png" width="512" height="512"/>
    <!-- see: https://www.npmjs.com/package/phonegap-plugin-push -->
    <gap:plugin name="phonegap-plugin-push" source="npm" version="1.2.3" />

    <!-- see: https://build.phonegap.com/plugins/1246 -->

    <gap:plugin name="com.phonegap.plugins.facebookconnect" version="0.8.0">
        <param name="APP_ID" value="1662727713975974" />
        <param name="APP_NAME" value="Reversi" />
    </gap:plugin>

    <!-- https://build.phonegap.com/plugins/2828 -->
    <gap:plugin name="it.zerob.cordova.screenshot" source="pgb" version="0.1.3" />
    <!-- https://github.com/EddyVerbruggen/SocialSharing-PhoneGap-Plugin -->
    <gap:plugin name="nl.x-services.plugins.socialsharing" source="pgb" version="4.3.0" />

    <!--
      See: https://github.com/apache/cordova-plugin-whitelist
      So you can load an iframe with the game platform.
    -->
    <access origin="*"/>
    <!-- Added the following intents to support the removal of whitelist code from base cordova to a plugin -->
    <!-- Whitelist configuration. Refer to https://cordova.apache.org/docs/en/edge/guide_appdev_whitelist_index.md.html -->
    <gap:plugin name="cordova-plugin-whitelist" source="npm" version="1.0.0" />
    <allow-navigation href="http://*/*" />
    <allow-navigation href="https://*/*" />
    <allow-intent href="*" />
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="tel:*" />
    <allow-intent href="sms:*" />
    <allow-intent href="mailto:*" />
    <allow-intent href="geo:*" />
    <platform name="android">
        <allow-intent href="market:*" />
    </platform>

    <gap:config-file platform="android" parent="/manifest/application" mode="add" >
      <activity
          android:configChanges="locale|keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
          android:name=".Reversi" android:label="@string/app_name"
          android:screenOrientation="unspecified"
          android:launchMode="singleTask"
          android:windowSoftInputMode="adjustUnspecified|stateUnspecified">
        <intent-filter>
          <action android:name="android.intent.action.MAIN"/>
          <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
        <intent-filter>
          <action android:name="android.intent.action.VIEW"/>
          <category android:name="android.intent.category.DEFAULT"/>
          <category android:name="android.intent.category.BROWSABLE"/>
          <data android:scheme="reversi"/>
        </intent-filter>
      </activity>
    </gap:config-file>
    <gap:url-scheme>
      <scheme>reversi</scheme>
    </gap:url-scheme>
</widget>
